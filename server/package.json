{
  "name": "quickdrop-signaling",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ephemeral WebRTC signaling for QuickDrop. File bytes never pass through this service.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env index.js",
    "start": "node --env-file-if-exists=.env index.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
